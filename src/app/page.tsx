import Hero from "@/components/sections/Hero";

export default function Home() {
  return (
    <main>
      <Hero />
      {/* Future batches: Identity Stack, Programs + HYROX + Schedule, Conversion, Close */}
    </main>
  );
}
